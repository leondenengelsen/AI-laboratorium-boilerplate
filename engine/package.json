{
  "name": "ai-engine",
  "version": "0.1.0",
  "description": "Reusable, domain-agnostic AI engine for calling AI providers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@google/generative-ai": "^0.21.0",
    "@mistralai/mistralai": "^1.5.0",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
